services:
  - type: web
    name: genome-quotation-pdf
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn genome_quotation_pdf_service:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
